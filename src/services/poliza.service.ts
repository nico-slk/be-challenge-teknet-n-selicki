import { Request, Response } from "express";

const getPoliza = async (req: Request, res: Response) => {

};

const postPoliza = async (req: Request, res: Response) => {
  const file = req.file;
  try {

  } catch (error) {

  }
};

export default {
  getPoliza,
  postPoliza
};
